# `mk/`

Reserved for Makefile includes when the command panel grows.

This repo keeps a single root `Makefile` for tutorial clarity, but the Chapter 10
topology expects orchestration to live under `mk/` and be included from the root.

